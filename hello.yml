# playbooks/hello.yml
- name: Hello sample
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    name: "{{ name | default('World') }}"

  tasks:
    - name: say hello
      ansible.builtin.debug:
        msg: "Hello, {{ name }}!"
